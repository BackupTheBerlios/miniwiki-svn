CREATE TABLE TBL_USER (
   ID IDENTITY,
   USERNAME VARCHAR(255), 
   PASSWORD VARCHAR(255)
);
ALTER TABLE TBL_USER ADD CONSTRAINT UNQ_TBL_USER_USERNAME UNIQUE (USERNAME);

CREATE TABLE TBL_LOAN_APPLICATION (
    ID IDENTITY,
	SURNAME VARCHAR(255), 
	FIRSTNAME VARCHAR(255), 
	AGE INTEGER, 
	ADDRESS VARCHAR(255), 
	IC_NUMBER VARCHAR(255), 
	EMAIL VARCHAR(255), 
	TELEPHONE_NUMBER VARCHAR(255), 
	HANDPHONE_NUMBER VARCHAR(255), 
	ANNUAL_INCOME DOUBLE,
    LOAN_AMOUNT DOUBLE, 
    REFERRAL_HP_NO VARCHAR(255), 
	APPLICATION_DATE TIMESTAMP
);


INSERT INTO TBL_USER(USERNAME, PASSWORD) VALUES ('admin', 'admin');
INSERT INTO TBL_LOAN_APPLICATION(SURNAME, FIRSTNAME, AGE, ADDRESS, IC_NUMBER, EMAIL, TELEPHONE_NUMBER, HANDPHONE_NUMBER, ANNUAL_INCOME, LOAN_AMOUNT, REFERRAL_HP_NO, APPLICATION_DATE) VALUES('s1', 'f1', null, 'a1', 'i1', 'e1', 't1', 'h1', null, null, 'r1', null);
