#
# core build properties
#
version=1.0
modules.dir=${basedir}/..
main.dir=${modules.dir}/..
conf.dir=${basedir}/conf
conf.metainf.dir=${conf.dir}/metainfo
conf.webinf.dir=${conf.dir}/webinfo
repository.dir=${main.dir}/repository
share.lib.dir=${repository.dir}/share
local.lib.dir=${repository.dir}/local
common.dir=${main.dir}/common
ivy.settings=${common.dir}/ivysettings.xml
ivy.conf=${basedir}/ivy.xml
lib.dir=${basedir}/lib
java.src.dir=${basedir}/java/src
java.resources.dir=${basedir}/java/resources
test.src.dir=${basedir}/test/src
test.resources.dir=${basedir}/test/resources
build.dir=${basedir}/build
java.classes.dir=${build.dir}/java/classes
test.classes.dir=${build.dir}/test/classes
test.report.dir=${build.dir}/reports
dist.dir=${build.dir}/dist
dist.filename=core.jar
dist.war.filename=web.war
www.dir=${build.dir}/www
scripts.dir=${build.dir}/scripts
tomcat.dir=${main.dir}/tomcat
tomcat.webapps.dir=${tomcat.dir}/webapps
tomcat.webapps.miniwiki.dir=${tomcat.webapps.dir}/miniwiki
tomcat.webapps.miniwiki.webinf.dir=${tomcat.webapps.miniwiki.dir}/WEB-INF
tomcat.webapps.miniwiki.classes.dir=${tomcat.webapps.miniwiki.webinf.dir}/classes
tomcat.webapps.miniwiki.classes.metainf.dir=${tomcat.webapps.miniwiki.classes.dir}/META-INF
tomcat.webapps.miniwiki.lib.dir=${tomcat.webapps.miniwiki.webinf.dir}/lib
tomcat.log.dir=${tomcat.dir}/logs

module.core.lib.dir=${modules.dir}/core/lib
module.radeox.lib.dir=${modules.dir}/radeox/lib
module.web.lib.dir=${modules.dir}/web/lib
module.core.resources.dir=${modules.dir}/core/java/resources
module.radeox.resources.dir=${modules.dir}/radeox/java/resources
module.web.resources.dir=${modules.dir}/web/java/resources

#gwt.dir=/home/tmjee/mojo/gwt/gwt-linux-1.5.0
gwt.dir=C:\\mojo\\gwt\\gwt-windows-1.5.0
gwt.shell.port=8080
